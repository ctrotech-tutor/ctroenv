{
  "name": "example-with-cli",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "validate": "ctroenv validate --source .env",
    "validate:watch": "ctroenv validate --watch --source .env",
    "generate": "ctroenv generate",
    "check": "ctroenv check",
    "docs": "ctroenv docs",
    "fix": "ctroenv validate --source .env && ctroenv generate && ctroenv docs"
  },
  "dependencies": {
    "@ctroenv/core": "^1.0.0",
    "@ctroenv/node": "^1.0.0"
  },
  "devDependencies": {
    "@ctroenv/cli": "^1.0.0",
    "tsx": "^4.19.0"
  }
}
